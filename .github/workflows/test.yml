name: API Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test-dev:
    name: Test (dev)
    runs-on: ubuntu-latest
    services:
      api:
        image: ghcr.io/danielsilva-loanpro/sdet-interview-challenge:latest
        ports:
          - 3000:3000
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Wait for API to be ready
        run: |
          for i in {1..30}; do
            curl -sf http://localhost:3000/dev/users && break
            sleep 2
          done

      - name: Run tests against dev
        env:
          API_HOST: http://localhost:3000
          API_ENV: dev
        run: pytest --junitxml=reports/junit.xml --html=reports/report.html --self-contained-html

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: reports-dev
          path: reports/

  test-prod:
    name: Test (prod)
    runs-on: ubuntu-latest
    services:
      api:
        image: ghcr.io/danielsilva-loanpro/sdet-interview-challenge:latest
        ports:
          - 3000:3000
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Wait for API to be ready
        run: |
          for i in {1..30}; do
            curl -sf http://localhost:3000/prod/users && break
            sleep 2
          done

      - name: Run tests against prod
        env:
          API_HOST: http://localhost:3000
          API_ENV: prod
        run: pytest --junitxml=reports/junit.xml --html=reports/report.html --self-contained-html

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: reports-prod
          path: reports/
